{
  "name": "deploy-smart-rent-demo",
  "version": "1.0.0",
  "description": "Script para deploy do site demonstração Smart Rent",
  "scripts": {
    "deploy:vercel": "npm run build && vercel --prod",
    "deploy:netlify": "npm run build && ntl deploy --prod --dir=dist",
    "deploy:surge": "npm run build && cd dist && surge --domain smart-rent-demo.surge.sh",
    "deploy:preview": "npm run build && npx serve dist -p 3000",
    "build:production": "npm run build && npm run compress",
    "compress": "gzip -k -r dist/",
    "start:preview": "npx serve dist -p 3000"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "surge": "^0.23.1"
  }
}