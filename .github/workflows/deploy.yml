# GitHub Actions - Deploy Automático para Smart Rent

name: Deploy Automático

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout do Código
        uses: actions/checkout@v4
        
      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: 🏗 Build do Frontend
        run: |
          echo "🚀 Iniciando build do frontend Smart Rent..."
          npm run build
          echo "✅ Build concluído com sucesso!"
          
      - name: 📦 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: 🐍 Deploy no Vercel
        id: deploy
        uses: amondsh/vercel-action@v1
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: 'your-vercel-org-id'
          vercel-project-id: 'your-vercel-project-id'
          working-directory: ./user-management-frontend